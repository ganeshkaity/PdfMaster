import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Add Watermark to PDF Online Free – Allyono Apps",
    description: "Add watermark to PDF online for free. Stamp text or image watermarks on your PDF files securely.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
