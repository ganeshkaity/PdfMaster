import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Compress PDF Online Free – Allyono Apps",
    description: "Compress PDF files online for free. Reduce PDF file size while maintaining quality. Secure client-side processing.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
