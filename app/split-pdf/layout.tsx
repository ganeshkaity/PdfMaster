import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Split PDF Online Free – Allyono Apps",
    description: "Split PDF files online for free. Extract pages or split PDF documents into multiple files securely in your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
