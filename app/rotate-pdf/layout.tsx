import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Rotate PDF Online Free – Allyono Apps",
    description: "Rotate PDF pages online for free. Permanently rotate PDF pages 90, 180, or 270 degrees securely.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
