import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Remove Pages PDF Online Free – Allyono Apps",
    description: "Remove PDF pages online for free. Delete specific pages from your PDF documents securely in your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
